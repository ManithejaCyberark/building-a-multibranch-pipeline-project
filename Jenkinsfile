pipeline {
 agent any
 stages{
   stage("fetch the code"){
    steps{
         echo "build success"
      }
   }
    stage("build"){
    steps{
         echo "build completed"
      }
   }
  stage("Deployed in production"){
    steps{
         echo "deployed completed"
      }
   }
 }
}
